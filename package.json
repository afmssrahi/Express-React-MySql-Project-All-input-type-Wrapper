{
  "name": "form-task",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    
    "start": "concurrently \"yarn --cwd ./server-for-fullFuom run start\" \"wait-on http://localhost:5000/ && yarn --cwd ./All-input-file-type-exercise-in-react run start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.0.0",
    "wait-on": "^6.0.0"
  }
}
